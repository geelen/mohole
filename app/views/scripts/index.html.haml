%h1== Listing #{@user.name.possessive unless @user.nil?} scripts

%table
  - @scripts.each do |script|
    %tr
      %td= script.base_uri
      %td= hamlink_to script
      %td= hamlink_to :edit, script
      %td= hamlink_to :destroy, script

= hamlink_to :new, Script
